name: common
description: module for common and utils classes.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^9.1.1
  get_it: ^8.0.3
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.11